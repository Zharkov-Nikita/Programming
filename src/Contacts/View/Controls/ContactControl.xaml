<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Controls" xmlns:viewmodel="clr-namespace:View.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainVM}"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="440">
    <Grid Background="White">
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="True">
                        <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self},
                            Path=(Validation.Errors)[0].ErrorContent}"/>
                        <Setter Property="Background" Value="Pink"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image
            Width="100"
            Height="100"
            VerticalAlignment="Top"
            Margin="5,34"
            Source="/user.png"/>
        <StackPanel Margin="5" Grid.Column="1">
            <Label Content="Name:" Height="26"/>
            <TextBox
                Height="26"
                TextWrapping="Wrap"
                Margin="3"
                IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="EditContact.Name" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Label Content="Phone Number:" Height="26"/>
            <TextBox
                Height="26"
                TextWrapping="Wrap"
                Margin="3"
                IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="EditContact.Phone" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Label Content="Email:" Height="26"/>
            <TextBox
                Height="26"
                TextWrapping="Wrap"
                Margin="3"
                IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="EditContact.Email" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
    </Grid>
</UserControl>
